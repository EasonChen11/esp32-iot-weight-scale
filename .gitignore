.claude
.pio
.vscode
nodered_data
.worktrees/
docs/

# PlatformIO compilation database (generated by `pio run` / clangd)
compile_commands.json

# Secrets (WiFi / MQTT credentials)
include/config_secrets.h

# Mosquitto runtime files (written by root inside Docker container)
docker/mosquitto/data/
docker/mosquitto/log/

CLAUDE.md
.claudeignore

# Superpowers workflow artifacts (spec/plan) — not committed
docs/superpowers/
