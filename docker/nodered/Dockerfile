FROM nodered/node-red:latest

# Pre-install the dashboard plugin so /ui works out of the box.
# node-red-dashboard provides ui_gauge, ui_chart, ui_text, etc.
RUN npm install --no-fund --no-update-notifier node-red-dashboard
